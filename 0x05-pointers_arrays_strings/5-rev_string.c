#include "main.h"
#include <string.h>

/**
  *rev_string - Entry point
  *@s: The character checks the output
  *Return: 0
  */
void rev_string(char *s)
{
	int len = strlen(s);
	int i = len - 1;


	while (i >= len)

	{
		len--;
	}

}
