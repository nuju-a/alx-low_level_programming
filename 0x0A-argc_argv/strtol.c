#include "main.h"

/**
  *_strtol - Entry point
  *@s: character to convert
  *@p: pointer character storage
  *@base: number base
  *
  *Return: 0
  */
long int _strtol(char *s, char **p, int base)
{
	int num = _strtol(str);

	return (num);
}

