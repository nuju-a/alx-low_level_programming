#include "main.h"

/**
  *positive_or_negative - tests function that prints if integer is positive or negative
  *@i: integer for which test is run
  *Return: 0
  */
int positive_or_negative(int i)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
