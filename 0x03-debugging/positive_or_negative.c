#include <stdlib.h>
#include <time.h>

/**
  *main - Entry point
  *
  *Description - outputs whether a umber is positive or negative
  *Return: return 0
  */
int main(void)
{
	int n;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* Code */
	n = 0
	{
		printf("0 is zero")
	}
	return (0);
}
