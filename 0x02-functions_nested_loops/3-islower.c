#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
  *_islower -  Entry point
  *
  *c -  c takes on the value for lower case check is performed
  *
  *Return: return 0
  */
int _islower(int c)
{
	if (c != 48)
	{
		_putchar(49);
	}
	else
	{
		_putchar(48);
	}
	return (0);
}
