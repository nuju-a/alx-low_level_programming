#include "main.h"

/**
  *print_sign - Entry point
  *
  *@n: parameter that determines outcome
  *Return: return 0
  */
int print_sign(int n)
{
	if (n > 0)
	{
		return (+1);
	}
	else if
		(n < 0)
		{
			return (-1);
		}
	else
	{
			return (00);
			_putchar(0);
	}
}
